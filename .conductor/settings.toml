"$schema" = "https://conductor.build/schemas/settings.repo.schema.json"

[scripts]
setup = "git submodule update --init --recursive || true"
run_mode = "concurrent"

[scripts.run.eval]
command = ".conductor/eval.sh"
default = true
icon = "test-tube"
